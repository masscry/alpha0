language: c
dist: trusty

compiler:
 - gcc
 - clang


before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq cmake

install: true

script:
- mkdir build
- cd build
- cmake ../
- cmake --build ./


